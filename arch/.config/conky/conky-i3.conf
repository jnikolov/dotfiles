conky.config = {
	out_to_x = false,
	own_window = false,
	out_to_console = true,
	background = false,
	max_text_width = 0,

-- Update interval in seconds
	update_interval = 0.8,

-- This is the number of times Conky will update before quitting.
-- Set to zero to run forever.
	total_run_times = 0,

-- Shortens units to a single character (kiB->k, GiB->G, etc.). Default is off.
	short_units = true,

-- How strict should if_up be when testing an interface for being up?
-- The value is one of up, link or address, to check for the interface
-- being solely up, being up and having link or being up, having link
-- and an assigned IP address.
	if_up_strictness = 'address',

-- Add spaces to keep things from moving about?  This only affects certain objects.
-- use_spacer should have an argument of left, right, or none
	use_spacer = 'left',

-- Force UTF8? note that UTF8 support required XFT
	override_utf8_locale = false,

-- number of cpu samples to average
-- set to 1 to disable averaging
	cpu_avg_samples = 10,
-- Stuff after 'TEXT' will be formatted on screen
-- Ligth theme (to change default color: replace 333333)
-- #333333 = arch color for light theme
-- #dddddd = arch color for dark theme

};

conky.text = [[
# JSON for i3bar

[

### MPD CONKY
# Any UTF8 JSON error may be cause by some invalid character in title/filename song

#{ "full_text" : ${if_mpd_playing}" mpd"${else}""${endif},  "separator":false, "separator_block_width":5, "color":"\#333333"},

#{ "full_text" :\
${if_mpd_playing}\
                ${if_match "${mpd_random}"=="On"}\
                ""\
                ${else} ""\
                ${endif}\
${else}""${endif},  "separator":false, "separator_block_width":3, "color" : ${if_match "${mpd_random}"=="On"}\
                "\#333333"\
                ${else} "\#999999"\
                ${endif}},
#{ "full_text" :\
${if_mpd_playing}\
                ${if_match "${mpd_repeat}"=="On"}\
                ""\
                ${else} ""\
                ${endif}\
${else}""${endif},  "separator":false, "separator_block_width":5, "color" : ${if_match "${mpd_repeat}"=="On"}\
                "\#333333"\
                ${else} "\#999999"\
                ${endif}},

#{ "full_text" : ${if_mpd_playing} ${if_match "${mpd_status}"=="Playing"}\
                                  " ${scroll 26 2 ${mpd_smart}} |"\
                                  ${else} " ${scroll 26 2 ${mpd_smart}} |"\
                                  ${endif}\
                ${else}""${endif},  "separator":false, "separator_block_width":8, "color":"\#333333"},
####


{ "full_text" : "${fs_free /home}",  "separator":false, "separator_block_width":8, "color" : ${if_match ${fs_free_perc /home}<=5}       "\#dc322f"  ${else} ${if_match ${fs_free_perc /home}<=10}"\#cb4b16" ${else} ${if_match ${fs_free_perc /home}<=15}"\#b58900"${else} "\#999999" ${endif} ${endif}${endif} },

{ "full_text" : " ${fs_free}",  "separator":false, "separator_block_width":8, "color" : ${if_match ${fs_free_perc}<=5}       "\#dc322f"  ${else} ${if_match ${fs_free_perc}<=10}"\#cb4b16" ${else} ${if_match ${fs_free_perc}<=15}"\#b58900"${else} "\#999999" ${endif} ${endif}${endif} },

{ "full_text" : ${if_match ${cpu cpu0}<10} " 0${cpu cpu0}%" ${else} " ${cpu cpu0}%" ${endif},       "separator":false, "separator_block_width":8, "color" : ${if_match ${cpu cpu0}>=90}       "\#dc322f"  ${else} ${if_match ${cpu cpu0}>=85}"\#cb4b16" ${else} ${if_match ${cpu cpu0}>=70}"\#b58900"${else} "\#999999" ${endif} ${endif} ${endif}},

#{ "full_text" : " ${exec ~/.bin/cpu_avg_temp.sh}°C",        "separator":false, "separator_block_width":8, "color" : ${if_match ${exec ~/.bin/cpu_avg_temp.sh}<=65} "\#999999"     ${else}  ${if_match ${exec ~/.bin/cpu_avg_temp.sh}>=75}"\#dc322fc" ${else} ${if_match ${exec ~/.bin/cpu_avg_temp.sh}>=70}"\#cb4b16"${else} "\#b58900" ${endif}${endif}  ${endif} },

{ "full_text" : " ${memperc}%",        "separator":false, "separator_block_width":8, "color" : ${if_match ${memperc}>90}       "\#dc322f"  ${else} ${if_match ${memperc}>=85}"\#cb4b16" ${else} ${if_match ${memperc}>=70}"\#b58900"${else} "\#999999" ${endif} ${endif}${endif} },
{ "full_text" : ${if_match "$gw_iface"=="(null)"} \
                " "\
                ${else} ${if_match ${upspeedf enp9s0}>=2.5}\
                            ${if_match ${downspeedf enp9s0}>=10.0}\
                            " "\
                            ${else} " "\
                            ${endif}\
                        ${else} ${if_match ${downspeedf enp9s0}>=10.0}\
                                " "\
                                ${else} " "\
                                ${endif}\
                        ${endif}\
${endif} ,  "separator":false, "separator_block_width":5, "color" : ${if_match "$gw_iface"=="(null)"}"\#dc322f"${else}"\#999999"${endif} },

{ "full_text" : ${if_match ${downspeedf enp9s0}>=10}\
                            ""\
                            ${else} ""\
                            ${endif}\
,  "separator":false, "separator_block_width":3, "color" :${if_match ${downspeedf enp9s0}>=10}\
                            "\#999999"\
                            ${else} "\#999999"\
                            ${endif}\
},
{ "full_text" : ${if_match ${upspeedf enp9s0}>=2.5}\
                            ""\
                            ${else} ""\
                            ${endif}\
,  "separator":false, "separator_block_width":8, "color" :${if_match ${upspeedf enp9s0}>=2.5}\
                            "\#999999"\
                            ${else} "\#999999"\
                            ${endif}\
},


#{ "full_text" : \
${if_match "${exec ~/.bin/alsa_conky.sh}"!="muted"}\
    ${if_match ${exec ~/.bin/alsa_conky.sh}>=85}\
    " ${exec ~/.bin/alsa_conky.sh}%"\
    ${else} ${if_match ${exec ~/.bin/alsa_conky.sh}==0}\
    " 0%"\
    ${else}" ${exec ~/.bin/alsa_conky.sh}%"${endif}${endif} ${else}" muted"${endif}\
    ,  "separator":false, "separator_block_width":8, "color" :\
#${if_match "${exec ~/.bin/alsa_conky.sh}"=="muted"}\
            "\#900000"\
    ${else} ${if_match ${exec ~/.bin/alsa_conky.sh}<=65}\
            "\#dc322f"\
    ${else} ${if_match ${exec ~/.bin/alsa_conky.sh}<=85}\
            "\#b58900"\
    ${else} "\#333333"\
            ${endif}\
            ${endif}\
${endif}},

{ "full_text" :\
	${if_match "${exec cat /sys/class/power_supply/BAT1/status}" == "Charging"}\
		""\
	${else}\
		${if_match ${exec cat /sys/class/power_supply/BAT1/capacity}>=95}\
			"", "color" : "\#999999"\
		${else}\
		${if_match ${exec cat /sys/class/power_supply/BAT1/capacity}>=75}\
			"", "color" : "\#66ff66"\
		${else}\
		${if_match ${exec cat /sys/class/power_supply/BAT1/capacity}>=50}\
			"", "color" : "\#009933"\
		${else}\
		${if_match ${exec cat /sys/class/power_supply/BAT1/capacity}>=25}\
			"", "color" : "\#ff9900"\
		${else}\
			"", "color" : "\#ff0000"\
		${endif}
		${endif}
		${endif}
		${endif}
	${endif}\
},


#${color red}$alignc Battery Information
#${color lightgrey} Currently: $battery
#${color lightgrey}$stippled_hr




# checkupdates for pacman!
#{ "full_text" : ${if_match ${execpi 3600 checkupdates | wc -l}<5}""${else}" ${execpi 60 checkupdates | wc -l}"${endif},  "separator":false, "separator_block_width":8, "color" : ${if_match ${execpi 3600 checkupdates | wc -l}==0}"\#000000"${else}"\#dc322f"${endif} },

#{ "full_text" : " ${execi 300 ~/.bin/weather.sh "SA|BR|SC|Florianopolis"}" ,  "separator":false, "separator_block_width":8, "color" : "\#999999" },
{ "full_text" :" ${time %a, %d %b} ${time %R}",     "separator":false, "separator_block_width":5, "color" : "\#999999" },
{ "full_text" :"",     "separator":false, "separator_block_width":8, "color" : "\#ffffff" }


],

]];

